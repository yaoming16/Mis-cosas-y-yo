import React from "react";
import ReactDOM from "react-dom/client";
import SearchPage from "../components/MusicPage";
import "../index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SearchPage />
  </React.StrictMode>
);
